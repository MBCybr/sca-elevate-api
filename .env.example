# Example .env file for SCA Elevate CLI
# Copy this to ".env" and fill in your tenant details

# CyberArk API base URL (must end with /api)
CYBERARK_API_BASE_URL=https://<subdomain>.sca.cyberark.cloud/api

# Optional defaults (uncomment and adjust if desired)
# ARK_DEFAULT_PROFILE=default
# AWS_ELEVATED_PROFILE=cyberark_elevated
